# Руководство по участию в проекте ChatList

Спасибо за интерес к проекту ChatList! Мы рады любым вкладам.

## 📋 Оглавление

- [С чего начать](#с-чего-начать)
- [Как внести вклад](#как-внести-вклад)
- [Стиль кода](#стиль-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)
- [Сообщения о багах](#сообщения-о-багах)
- [Предложения](#предложения)

## 🚀 С чего начать

1. **Fork** репозиторий
2. **Clone** свой fork:
   ```powershell
   git clone https://github.com/YOUR_USERNAME/ChatList.git
   cd ChatList
   ```
3. Создайте **виртуальное окружение**:
   ```powershell
   python -m venv venv
   .\venv\Scripts\activate
   ```
4. Установите **зависимости**:
   ```powershell
   pip install -r requirements.txt
   ```
5. Создайте файл **`.env`** с вашими API ключами

## 💡 Как внести вклад

### Сообщения о багах

Используйте [шаблон bug report](.github/ISSUE_TEMPLATE/bug_report.md):

1. Откройте [новый Issue](https://github.com/Evgen018/ChatList/issues/new/choose)
2. Выберите "🐛 Сообщить о проблеме"
3. Заполните все поля
4. Приложите логи из папки `logs/`

### Предложения улучшений

Используйте [шаблон feature request](.github/ISSUE_TEMPLATE/feature_request.md):

1. Откройте [новый Issue](https://github.com/Evgen018/ChatList/issues/new/choose)
2. Выберите "✨ Предложить функцию"
3. Опишите предложение подробно

### Изменения кода

1. **Создайте ветку** для ваших изменений:
   ```powershell
   git checkout -b feature/amazing-feature
   ```

2. **Внесите изменения** и протестируйте их:
   ```powershell
   python main.py
   ```

3. **Закоммитьте** изменения:
   ```powershell
   git add .
   git commit -m "Добавлена удивительная функция"
   ```

4. **Запушьте** в ваш fork:
   ```powershell
   git push origin feature/amazing-feature
   ```

5. **Откройте Pull Request** в основной репозиторий

## 🎨 Стиль кода

### Python

- Следуйте [PEP 8](https://peps.python.org/pep-0008/)
- Используйте **4 пробела** для отступов
- Максимальная длина строки: **100 символов**
- Используйте **type hints** где возможно
- Документируйте функции с помощью **docstrings**

Пример:

```python
def process_prompt(prompt: str, model_name: str) -> dict:
    """
    Обрабатывает промпт с помощью указанной модели.
    
    Args:
        prompt: Текст промпта для обработки
        model_name: Название модели для использования
        
    Returns:
        Словарь с результатом обработки
        
    Raises:
        ValueError: Если промпт пустой
        APIError: Если произошла ошибка API
    """
    if not prompt:
        raise ValueError("Промпт не может быть пустым")
    
    # Ваш код здесь
    return {"result": "..."}
```

### PyQt5

- Используйте **snake_case** для методов
- Используйте **CamelCase** для классов виджетов
- Группируйте сигналы и слоты логически

### Импорты

Порядок импортов:

```python
# 1. Стандартная библиотека
import os
import sys
from typing import Optional

# 2. Сторонние библиотеки
from PyQt5.QtWidgets import QWidget
from PyQt5.QtCore import Qt

# 3. Локальные импорты
from db import Database
from models import ModelManager
```

## 📝 Коммиты

### Формат сообщений

Используйте префиксы для коммитов:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `perf:` - улучшение производительности
- `test:` - добавление тестов
- `chore:` - рутинные задачи

Примеры:

```
feat: добавлена поддержка модели Claude 3 Opus
fix: исправлена ошибка при сохранении настроек
docs: обновлён README с инструкциями по установке
```

### Рекомендации

- Пишите сообщения **на русском языке**
- Используйте **повелительное наклонение**
- Первая строка до **50 символов**
- Подробное описание после пустой строки

## 🔀 Pull Requests

### Требования

- [ ] PR связан с существующим Issue
- [ ] Код соответствует стилю проекта
- [ ] Все изменения протестированы
- [ ] Документация обновлена
- [ ] Нет конфликтов с main веткой

### Процесс ревью

1. Создайте PR с описанием изменений
2. Дождитесь ревью от мейнтейнеров
3. Внесите запрошенные изменения
4. После одобрения PR будет смержен

## 🐛 Сообщения о багах

При сообщении о баге включите:

1. **Версию** приложения (из "О программе")
2. **ОС** и её версию
3. **Шаги** для воспроизведения
4. **Ожидаемое** и **фактическое** поведение
5. **Логи** из папки `logs/`
6. **Скриншоты** (если применимо)

## 💡 Предложения

При предложении новой функции:

1. **Опишите проблему**, которую она решает
2. **Предложите решение** с деталями
3. **Рассмотрите альтернативы**
4. **Приведите примеры** из других приложений

## 🧪 Тестирование

Перед созданием PR протестируйте:

1. Запуск приложения
2. Основной функционал
3. Ваши изменения
4. Темы (светлая и тёмная)
5. Различные размеры шрифта

## 📚 Документация

При добавлении функций обновите:

- `README.md` - основное описание
- `PROJECT.md` - технические детали
- `DATABASE.md` - изменения в БД
- `CHANGELOG.md` - список изменений
- Docstrings в коде

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [Issues](https://github.com/Evgen018/ChatList/issues)
2. Посмотрите [Discussions](https://github.com/Evgen018/ChatList/discussions)
3. Создайте новый Discussion для обсуждения

## 📜 Лицензия

Отправляя Pull Request, вы соглашаетесь с тем, что ваш код будет лицензирован под [MIT License](../LICENSE).

## 🙏 Благодарности

Спасибо за ваш вклад в проект ChatList! 🎉

---

**Нужна помощь?** Создайте [новый Discussion](https://github.com/Evgen018/ChatList/discussions/new)!
