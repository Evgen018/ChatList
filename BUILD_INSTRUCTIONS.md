# Инструкция по сборке ChatList

## 📋 Пошаговая инструкция для внесения изменений в исполняемый файл и инсталлятор

### ⚠️ ВАЖНО: Перед началом
Убедитесь, что:
- ✅ Все изменения в коде закоммичены в Git
- ✅ Установлены все зависимости (`pip install -r requirements.txt`)
- ✅ Установлен PyInstaller (`pip install pyinstaller`)
- ✅ Установлен Inno Setup (для создания инсталлятора)

---

## 🔢 ШАГ 1: Обновление версии

### 1.1. Откройте файл `version.py`
```powershell
notepad version.py
```

### 1.2. Обновите версию
Измените строку:
```python
__version__ = "1.0.2"
```
на:
```python
__version__ = "1.0.3"
```

### 1.3. Сохраните файл

---

## 🔨 ШАГ 2: Обновление Inno Setup скрипта

### 2.1. Откройте файл `ChatList.iss`
```powershell
notepad ChatList.iss
```

### 2.2. Обновите версию в двух местах:
```iss
#define MyAppVersion "1.0.3"
#define MyAppExeName "ChatList-v1.0.3.exe"
```

### 2.3. Сохраните файл

---

## 🏗️ ШАГ 3: Сборка исполняемого файла (.exe)

### 3.1. Активируйте виртуальное окружение (если используется)
```powershell
.\venv\Scripts\Activate.ps1
```

### 3.2. Очистите старые файлы сборки (опционально)
```powershell
Remove-Item -Path "build" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Path "dist\ChatList-v*.exe" -Force -ErrorAction SilentlyContinue
```

### 3.3. Запустите PyInstaller
```powershell
pyinstaller ChatList.spec
```

### 3.4. Проверьте результат
После завершения проверьте, что файл создан:
```powershell
Get-Item "dist\ChatList-v1.0.3.exe"
```

**Ожидаемый результат:**
- ✅ Файл `dist\ChatList-v1.0.3.exe` создан
- ✅ Размер файла ~40-50 МБ

---

## 📦 ШАГ 4: Создание инсталлятора

### 4.1. Откройте Inno Setup Compiler
- Найдите `Inno Setup Compiler` в меню Пуск
- Или откройте файл `ChatList.iss` двойным кликом (если настроена ассоциация)

### 4.2. Скомпилируйте инсталлятор
- В Inno Setup Compiler нажмите **Build → Compile** (или `F9`)
- Дождитесь завершения компиляции

### 4.3. Проверьте результат
```powershell
Get-Item "dist\ChatList-v1.0.3-Setup.exe"
```

**Ожидаемый результат:**
- ✅ Файл `dist\ChatList-v1.0.3-Setup.exe` создан
- ✅ Размер файла ~30-40 МБ

---

## 📚 ШАГ 5: Создание архивов исходного кода

### 5.1. Запустите скрипт создания архивов
```powershell
.\create_archives.ps1 -Version "1.0.3"
```

### 5.2. Проверьте результат
```powershell
Get-Item "dist\*1.0.3-Source*"
```

**Ожидаемый результат:**
- ✅ `dist\ChatList-v1.0.3-Source.zip`
- ✅ `dist\ChatList-v1.0.3-Source.tar.gz`

---

## ✅ ШАГ 6: Проверка всех файлов

### 6.1. Проверьте содержимое папки `dist`
```powershell
Get-ChildItem dist\*1.0.3* | Select-Object Name, @{Name="Size (MB)";Expression={[math]::Round($_.Length/1MB, 2)}}
```

**Ожидаемые файлы:**
1. ✅ `ChatList-v1.0.3.exe` (~40-50 МБ)
2. ✅ `ChatList-v1.0.3-Setup.exe` (~30-40 МБ)
3. ✅ `ChatList-v1.0.3-Source.zip` (~100-200 КБ)
4. ✅ `ChatList-v1.0.3-Source.tar.gz` (~50-100 КБ)

---

## 🚀 ШАГ 7: Тестирование

### 7.1. Протестируйте исполняемый файл
```powershell
.\dist\ChatList-v1.0.3.exe
```

**Проверьте:**
- ✅ Приложение запускается
- ✅ Новый визуальный индикатор вкладок работает в светлой теме
- ✅ Все функции работают корректно

### 7.2. Протестируйте инсталлятор
```powershell
.\dist\ChatList-v1.0.3-Setup.exe
```

**Проверьте:**
- ✅ Установка проходит успешно
- ✅ Приложение запускается после установки
- ✅ Ярлыки созданы корректно

---

## 📤 ШАГ 8: Коммит изменений в Git

### 8.1. Добавьте изменённые файлы
```powershell
git add version.py ChatList.iss main.py
```

### 8.2. Создайте коммит
```powershell
git commit -m "Update to v1.0.3: Add visual indicator for selected tab in light theme"
```

### 8.3. Отправьте изменения
```powershell
git push origin main
```

---

## 🌐 ШАГ 9: Создание GitHub Release

### 9.1. Перейдите на GitHub
Откройте: https://github.com/Evgen018/ChatList/releases/new

### 9.2. Заполните информацию о релизе

**Tag:** `v1.0.3`
- Выберите "Create new tag: v1.0.3"
- Выберите "Target: main"

**Release title:** `ChatList v1.0.3`

**Description:**
```markdown
## 🎨 Улучшения интерфейса

- ✨ Добавлен визуальный индикатор выбранной вкладки в светлой теме
  - Синее подчёркивание выбранной вкладки
  - Синий цвет текста и жирный шрифт для лучшей видимости

## 📦 Файлы для скачивания

- **ChatList-v1.0.3-Setup.exe** - Инсталлятор (рекомендуется)
- **ChatList-v1.0.3.exe** - Portable версия
- **ChatList-v1.0.3-Source.zip** - Исходный код (ZIP)
- **ChatList-v1.0.3-Source.tar.gz** - Исходный код (TAR.GZ)
```

### 9.3. Загрузите файлы
Перетащите в область "Attach binaries" следующие файлы из папки `dist`:
1. `ChatList-v1.0.3-Setup.exe`
2. `ChatList-v1.0.3.exe`
3. `ChatList-v1.0.3-Source.zip`
4. `ChatList-v1.0.3-Source.tar.gz`

### 9.4. Опубликуйте релиз
- Нажмите кнопку **"Publish release"**

---

## 📝 ШАГ 10: Обновление GitHub Pages (опционально)

Если нужно обновить ссылки на сайте:

### 10.1. Откройте `docs/index.html`
```powershell
notepad docs\index.html
```

### 10.2. Обновите версию в ссылках
Найдите и замените все упоминания `v1.0.2` на `v1.0.3`:
- В кнопках скачивания
- В тексте версии

### 10.3. Сохраните и закоммитьте
```powershell
git add docs/index.html
git commit -m "Update version to 1.0.3 on GitHub Pages"
git push origin main
```

---

## ✅ Чеклист финальной проверки

- [ ] Версия обновлена в `version.py`
- [ ] Версия обновлена в `ChatList.iss`
- [ ] Исполняемый файл `.exe` создан и протестирован
- [ ] Инсталлятор `.exe` создан и протестирован
- [ ] Архивы исходного кода созданы
- [ ] Все изменения закоммичены в Git
- [ ] GitHub Release создан с правильными файлами
- [ ] GitHub Pages обновлён (если нужно)

---

## 🆘 Решение проблем

### Проблема: PyInstaller не находит модули
**Решение:**
```powershell
pip install --upgrade pyinstaller
pip install -r requirements.txt
```

### Проблема: Inno Setup не компилирует
**Решение:**
- Убедитесь, что файл `dist\ChatList-v1.0.3.exe` существует
- Проверьте пути в `ChatList.iss`

### Проблема: Файлы слишком большие
**Решение:**
- Используйте UPX для сжатия (уже включено в `ChatList.spec`)
- Проверьте, что не включены лишние файлы

---

## 📞 Дополнительная информация

- **Документация:** `README.md`
- **Инструкции по релизу:** `RELEASE.md`
- **Чеклист публикации:** `PUBLISH_CHECKLIST.md`
