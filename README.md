# ChatList

Приложение для отправки одного и того же промпта в несколько нейросетей и сравнения их ответов.

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![PyQt5](https://img.shields.io/badge/GUI-PyQt5-green.svg)
![SQLite](https://img.shields.io/badge/DB-SQLite-orange.svg)

## Возможности

- 🚀 Отправка промпта в несколько нейросетей одновременно
- 📊 Сравнение ответов в удобной таблице
- 💾 Сохранение выбранных результатов в базу данных
- 📚 История всех сохранённых ответов с пагинацией
- 📄 Экспорт в Markdown и JSON
- 🔍 Поиск и сортировка во всех таблицах
- 📝 Сохранение и повторное использование промптов
- ✨ AI-ассистент для улучшения промптов
- 🎨 Светлая и тёмная темы оформления
- 🔧 Настройка размера шрифта

## Поддерживаемые провайдеры

| Провайдер | API | Модели |
|-----------|-----|--------|
| OpenAI | ✅ | GPT-4o, GPT-4o-mini и др. |
| Anthropic | ✅ | Claude 3.5 Sonnet, Claude Opus и др. |
| Google | ✅ | Gemini 1.5 Pro, Gemini 2.0 Flash |
| DeepSeek | ✅ | DeepSeek Chat, DeepSeek R1 |
| Groq | ✅ | Llama 3.1, Llama 3.3 |
| OpenRouter | ✅ | 500+ моделей через единый API |

## Установка

### 1. Клонирование репозитория

```powershell
git clone https://github.com/Evgen018/ChatList.git
cd ChatList
```

### 2. Установка зависимостей

```powershell
pip install -r requirements.txt
```

### 3. Настройка API-ключей

Создайте файл `.env` на основе примера:

```powershell
Copy-Item .env.example .env
```

Откройте `.env` и добавьте ваши API-ключи:

```env
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
OPENROUTER_API_KEY=sk-or-...
# и другие...
```

### 4. Запуск

**Вариант 1: Из исходников**
```powershell
python main.py
```

**Вариант 2: Исполняемый файл**
```powershell
# Сборка exe файла
pyinstaller ChatList.spec --clean

# Запуск готового приложения
.\dist\ChatList.exe
```

## Использование

### Вкладка «Запрос»
1. Введите промпт в текстовое поле
2. Нажмите «✨ Улучшить» для AI-улучшения промпта (опционально)
3. Нажмите «🚀 Отправить»
4. Дождитесь ответов от всех активных моделей

### Вкладка «Результаты»
1. Просмотрите ответы всех моделей
2. Отметьте чекбоксами нужные результаты
3. Нажмите «💾 Сохранить выбранные»

### Вкладка «Модели»
1. Нажмите «📋 Добавить модели по умолчанию»
2. Активируйте нужные модели галочками
3. Добавляйте свои модели через форму

### Вкладка «История»
1. Просматривайте сохранённые результаты
2. Используйте поиск для фильтрации
3. Экспортируйте в Markdown или JSON

### Вкладка «Настройки»
1. Выберите тему: ☀️ Светлая или 🌙 Тёмная
2. Настройте размер шрифта (8-24 пт)
3. Выберите модель для AI-улучшения промптов
4. Настройте таймаут и лимит токенов

## Структура проекта

```
ChatList/
├── main.py           # Главное окно приложения (PyQt5)
├── db.py             # Работа с базой данных SQLite
├── network.py        # API-клиенты для нейросетей
├── models.py         # Логика работы с моделями
├── logger.py         # Логирование запросов
├── version.py        # Версия приложения
├── create_ico.py     # Генерация иконки приложения
├── app.ico           # Иконка приложения
├── app_icon.png      # PNG версия иконки
├── requirements.txt  # Зависимости Python
├── .env.example      # Пример файла окружения
├── .env              # API-ключи (не в git)
├── chatlist.db       # База данных (создаётся при запуске)
├── logs/             # Логи приложения
├── DATABASE.md       # Схема базы данных
├── PLAN.md           # План разработки
└── PROJECT.md        # Спецификация проекта
```

## Логирование

Логи сохраняются в папке `logs/` с разбивкой по дням:
- `chatlist_2026-01-08.log`

## Сборка исполняемого файла

Для создания автономного exe файла:

```powershell
# Установка PyInstaller (если ещё не установлен)
pip install pyinstaller

# Сборка
pyinstaller ChatList.spec --clean
```

Готовый файл будет находиться в `dist/`:
- `ChatList-v1.0.0.exe` — с версией в имени
- `ChatList.exe` — для удобства запуска

Размер: ~43 МБ (включает все зависимости и Python runtime)

## Управление версией

Версия приложения хранится в файле `version.py`:

```python
__version__ = "1.0.0"
```

**Для изменения версии приложения достаточно отредактировать только один файл — `version.py`.**

Версия автоматически обновится:
- В интерфейсе (окно "О программе")
- В логах приложения
- Во всех местах, где используется версия

## Создание инсталлятора

Для создания инсталлятора используется **Inno Setup**.

### Требования
1. Скачайте и установите [Inno Setup](https://jrsoftware.org/isdl.php) (версия 6.x или выше)
2. Убедитесь, что исполняемый файл уже создан (см. раздел "Создание исполняемого файла")

### Создание инсталлятора

#### Через GUI Inno Setup:
1. Откройте файл `ChatList.iss` в **Inno Setup Compiler**
2. Нажмите **Build → Compile** (или F9)
3. Инсталлятор будет создан в папке `dist/`:
   - `ChatList-v1.0.0-Setup.exe`

#### Через командную строку:
```powershell
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ChatList.iss
```

### Что включает инсталлятор
- ✅ Исполняемый файл `ChatList-v1.0.0.exe`
- ✅ Базу данных `chatlist.db` (создаётся при первом запуске)
- ✅ Иконки приложения (`app.ico`, `app_icon.png`)
- ✅ Документацию (`README.md`, `LICENSE`)
- ✅ Пример настроек (`.env.example`)
- ✅ Ярлыки в меню "Пуск"
- ✅ Опциональный ярлык на рабочем столе
- ✅ Полная деинсталляция с опцией сохранения данных

### Особенности деинсталляции
При удалении программы пользователю будет предложено:
- **Сохранить базу данных и настройки** — для переноса или обновления
- **Удалить всё** — полная очистка системы

## Лицензия

MIT License

## Автор

[Evgen018](https://github.com/Evgen018)
