# 📦 Инструкция по публикации ChatList на GitHub

## Оглавление
1. [Подготовка к релизу](#подготовка-к-релизу)
2. [Создание GitHub Release](#создание-github-release)
3. [Настройка GitHub Pages](#настройка-github-pages)
4. [Автоматизация](#автоматизация)

---

## 🎯 Подготовка к релизу

### Шаг 1: Проверка версии
Убедитесь, что версия установлена правильно:

```powershell
# Проверьте version.py
Get-Content version.py
```

Должно быть: `__version__ = "1.0.0"`

### Шаг 2: Создание исполняемого файла

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\activate

# Создайте исполняемый файл
pyinstaller ChatList.spec
```

**Результат:** `dist\ChatList-v1.0.0.exe` (~43 МБ)

### Шаг 3: Создание инсталлятора

Откройте `ChatList.iss` в **Inno Setup Compiler** и нажмите **F9** (Compile).

**Результат:** `dist\ChatList-v1.0.0-Setup.exe` (~43 МБ)

### Шаг 4: Тестирование

```powershell
# Запустите исполняемый файл
.\dist\ChatList-v1.0.0.exe

# Проверьте инсталлятор
.\dist\ChatList-v1.0.0-Setup.exe
```

---

## 🚀 Создание GitHub Release

### Шаг 1: Создание Git-тега

```powershell
# Убедитесь, что все изменения закоммичены
git status

# Если есть незакоммиченные изменения:
git add .
git commit -m "Подготовка к релизу v1.0.0"

# Создайте тег
git tag -a v1.0.0 -m "Релиз версии 1.0.0"

# Отправьте изменения и тег на GitHub
git push origin main
git push origin v1.0.0
```

### Шаг 2: Создание Release на GitHub

1. **Откройте страницу релизов:**
   - Перейдите на https://github.com/Evgen018/ChatList/releases
   - Нажмите **"Create a new release"** (или **"Draft a new release"**)

2. **Заполните форму:**

   **Choose a tag:** `v1.0.0` (выберите созданный тег)
   
   **Release title:** `ChatList v1.0.0 - Первый релиз`
   
   **Description:** Используйте шаблон из `.github/release-template.md`:

   ```markdown
   ## 🎉 ChatList v1.0.0 - Первый публичный релиз

   **ChatList** — современное десктопное приложение для работы с AI-моделями через единый интерфейс.

   ### ✨ Основные возможности

   - 🤖 **Поддержка популярных AI-моделей**:
     - OpenAI (GPT-4, GPT-3.5)
     - Anthropic Claude
     - Google Gemini
     - OpenRouter (доступ к 100+ моделям)
   
   - 📝 **Улучшение промптов**:
     - Автоматическая оптимизация запросов
     - История всех запросов и результатов
     - Экспорт в Markdown
   
   - 🎨 **Удобный интерфейс**:
     - Светлая и тёмная темы
     - Настройка размера шрифта
     - Современный Material Design
   
   - 💾 **База данных**:
     - SQLite для хранения всех данных
     - Быстрый поиск по истории
     - Управление моделями

   ### 📦 Установка

   #### Вариант 1: Инсталлятор (рекомендуется)
   Скачайте `ChatList-v1.0.0-Setup.exe` и запустите установку.

   #### Вариант 2: Portable версия
   Скачайте `ChatList-v1.0.0.exe` и запустите без установки.

   ### ⚙️ Настройка

   1. Создайте файл `.env` в папке с программой:
   ```env
   OPENAI_API_KEY=ваш_ключ
   ANTHROPIC_API_KEY=ваш_ключ
   GOOGLE_API_KEY=ваш_ключ
   OPENROUTER_API_KEY=ваш_ключ
   ```

   2. Пример файла `.env.example` включён в поставку.

   ### 📊 Системные требования

   - **ОС:** Windows 10/11 (64-bit)
   - **ОЗУ:** Минимум 256 МБ
   - **Место на диске:** 100 МБ

   ### 🐛 Известные проблемы

   - Нет поддержки macOS и Linux (планируется в будущем)

   ### 📝 Изменения

   Первый релиз — все функции новые!

   ### 🙏 Благодарности

   Спасибо всем, кто тестировал и давал обратную связь!

   ---

   **Полная документация:** [GitHub Pages](https://evgen018.github.io/ChatList/)
   **Исходный код:** [GitHub Repository](https://github.com/Evgen018/ChatList)
   ```

3. **Загрузите файлы:**
   
   Перетащите следующие файлы в раздел **"Attach binaries"**:
   - `dist\ChatList-v1.0.0-Setup.exe` (инсталлятор, ~43 МБ)
   - `dist\ChatList-v1.0.0.exe` (portable версия, ~41 МБ)
   - `dist\ChatList-v1.0.0-Source.zip` (исходный код ZIP, ~93 КБ)
   - `dist\ChatList-v1.0.0-Source.tar.gz` (исходный код TAR.GZ, ~78 КБ)
   - `LICENSE` (файл лицензии)
   - `README.md` (документация)

4. **Опции релиза:**
   - ☑️ **Set as the latest release** (отметьте)
   - ☐ **Set as a pre-release** (НЕ отмечайте для стабильной версии)

5. **Публикация:**
   - Нажмите **"Publish release"**

---

## 🌐 Настройка GitHub Pages

### Шаг 1: Создание структуры документации

Файлы уже созданы в папке `docs/`:
- `index.html` — главная страница
- `style.css` — стили
- `logo.png` — логотип (скопируйте `app_icon.png`)

### Шаг 2: Подготовка ресурсов

```powershell
# Скопируйте иконку для сайта
Copy-Item app_icon.png docs\logo.png

# Добавьте скриншоты (если есть)
# New-Item -ItemType Directory -Path docs\screenshots -Force
# Copy-Item screenshots\*.png docs\screenshots\
```

### Шаг 3: Коммит и пуш

```powershell
git add docs/
git commit -m "Добавлена документация для GitHub Pages"
git push origin main
```

### Шаг 4: Включение GitHub Pages

1. Откройте репозиторий: https://github.com/Evgen018/ChatList
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Branch:** `main`
   - **Folder:** `/docs`
4. Нажмите **Save**

Через несколько минут сайт будет доступен по адресу:
**https://evgen018.github.io/ChatList/**

---

## 🤖 Автоматизация (опционально)

### GitHub Actions для автоматического создания релизов

Если вы хотите автоматизировать процесс, используйте файл `.github/workflows/release.yml` (уже создан).

**Как это работает:**
1. Создаёте тег: `git tag v1.0.1 && git push origin v1.0.1`
2. GitHub автоматически собирает билд и создаёт релиз

**Примечание:** Для Windows-сборки потребуется настроить Windows runner или использовать self-hosted runner.

---

## 📋 Чеклист перед релизом

Перед каждым релизом проверьте:

- [ ] Версия в `version.py` обновлена
- [ ] `README.md` актуален
- [ ] `CHANGELOG.md` содержит описание изменений
- [ ] Все тесты пройдены
- [ ] Исполняемый файл создан и протестирован
- [ ] Инсталлятор создан и протестирован
- [ ] Все изменения закоммичены
- [ ] Тег создан и отправлен на GitHub
- [ ] Release Notes подготовлены
- [ ] Файлы загружены в GitHub Release
- [ ] GitHub Pages обновлён

---

## 🔄 Обновление версии

Для следующих релизов:

1. **Обновите версию:**
   ```python
   # version.py
   __version__ = "1.0.1"
   ```

2. **Создайте новый билд:**
   ```powershell
   pyinstaller ChatList.spec
   ```

3. **Создайте новый инсталлятор:**
   - Откройте `ChatList.iss` в Inno Setup
   - Обновите версию (она автоматически берётся из `version.py`)
   - Нажмите F9

4. **Создайте тег и релиз:**
   ```powershell
   git tag -a v1.0.1 -m "Релиз версии 1.0.1"
   git push origin v1.0.1
   ```

5. **Создайте GitHub Release** (см. инструкцию выше)

---

## 📞 Поддержка

Если возникли вопросы:
- Создайте Issue: https://github.com/Evgen018/ChatList/issues
- Email: [ваш email]

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)
